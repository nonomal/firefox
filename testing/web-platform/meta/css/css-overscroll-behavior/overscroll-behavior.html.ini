[overscroll-behavior.html]
  prefs: [mousewheel.transaction.timeout:0]
  expected:
    if (os == "mac") and (processor == "x86_64"): TIMEOUT
  [overscroll-behavior-y: none prevents scroll propagation on y axis]
    expected:
      if os == "android": [PASS, FAIL]

  [overscroll-behavior allows inner scrolling when both axes are none]
    expected:
      if (os == "mac") and (version == "OS X 15.3") and (processor == "x86_64"): [NOTRUN, TIMEOUT]
      if (os == "mac") and (version == "OS X 14.7.5"): [NOTRUN, TIMEOUT]
      if os == "android": [PASS, FAIL]

  [overscroll-behavior on non-scrollable area allows scroll propagation]
    expected:
      if (os == "mac") and (processor == "x86_64"): NOTRUN
